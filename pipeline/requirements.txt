# World-Aligned 3D Asset Generation Pipeline – Requirements
# Install with: pip install -r requirements.txt

# Modal platform
modal>=0.62

# Gemini LLM (new unified SDK – google-generativeai is deprecated)
google-genai>=0.5.0

# Retry logic
tenacity>=8.2

# 3D mesh processing
trimesh>=4.3
scipy>=1.12
pygltflib>=1.16

# Numerics
numpy>=1.26

# Image processing
pillow>=10.3

# CLI output
rich>=13.7

# Web API (needed locally because FastAPI is imported at module level)
fastapi>=0.110
pydantic>=2.0

# ── Dev / testing ─────────────────────────────────────────────────────────────
pytest>=8.1
pytest-mock>=3.14
